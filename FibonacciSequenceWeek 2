package week2;

/*In this assignment I will be using a formula that will help me
 * make a list using fibonacci sequence. 
 * This list will not exceed over 10000
 * and will use a while loop with the constant being MAX_TERM_VALUE .
 * I will import the acm.program.*
 * the public class will be Fibonacci
 * which will extend into the console program
 * and that will display the list. I created.
 */
import acm.program.*;

public class Fibonacci extends ConsoleProgram {

/* explains the largest term that will be displayed*/
		
	private static final int MAX_TERM_VALUE = 10000;
		
		public void run() {
			int n1 = 0;
			int n2 = 1;
			println( " This program lists the Fibonacci sequence.");
			while (n1 <= MAX_TERM_VALUE) {
				println(n1);
				int n3 = n1 + n2;
				n1 = n2;
				n2 = n3;
			}
		}
		
		
		
	}
